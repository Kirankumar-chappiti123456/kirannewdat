MSB: of a given number: without loop:
in bitwise operators in c:
#include<stdio.h>
#define BITS sizeof(int) * 4
int main()
{
    int a=0,msb=0,bit=4;
    printf("enter the number:");
    scanf("%d",&a);
    msb=1<<(bit-1);
    (a&msb)?printf("MSB"):printf("NOT MSb");
    
}
LSB:
#include<stdio.h>
int main()
{
    int a=0,lsb=0;
    printf("enter the number:");
    scanf("%d",&a);
    lsb=(1&a);
    (lsb==1)?printf("LSB"):printf("NOT LSB");
    
}
