MSB: of a given number:
in bitwise operators in c:
#include<stdio.h>
#define BITS sizeof(int) * 4
int main()
{
    int a=0,msb=0,bit=4;
    printf("enter the number:");
    scanf("%d",&a);
    msb=1<<(bit-1);
    (a&msb)?printf("MSB"):printf("NOT MSb");
    
}
